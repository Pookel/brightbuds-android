# BrightBuds Android

Android learning app for early learners.
Built with Android Studio, Java, and Firebase (Auth, Firestore, Storage).

## Setup
1. Clone with SSH
2. Open in Android Studio
3. Add `google-services.json` inside the `app/` folder
4. Sync Gradle and Run
