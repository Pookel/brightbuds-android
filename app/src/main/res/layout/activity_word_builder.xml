<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.WordBuilderActivity">

    <!-- Background (same style as Match Letter) -->
    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/background_match_letter_2"
        android:contentDescription="@string/content_desc_background" />

    <!-- Confetti layer -->
    <nl.dionsegijn.konfetti.xml.KonfettiView
        android:id="@+id/konfettiView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Home button -->
    <ImageView
        android:id="@+id/homeButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/home_icon"
        android:contentDescription="@string/content_desc_home"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Close button -->
    <ImageView
        android:id="@+id/closeButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginEnd="24dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/close_icon"
        android:contentDescription="@string/content_desc_close"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 🐶 Cute character (from Match Letter module) -->
    <ImageView
        android:id="@+id/characterView"
        android:layout_width="110dp"
        android:layout_height="110dp"
        app:layout_constraintBottom_toTopOf="@+id/wordGameContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/homeButton"
        app:layout_constraintVertical_bias="0.2" />

    <!-- 📚 Word Builder game area (middle) -->
    <LinearLayout
        android:id="@+id/wordGameContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="24dp"
        android:paddingTop="16dp"
        android:paddingBottom="32dp"
        app:layout_constraintTop_toBottomOf="@id/characterView"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 🧩 Word slots (top) -->
        <LinearLayout
            android:id="@+id/llWordContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginBottom="24dp"
            android:clipToPadding="false" />

        <!-- Letter choices (bottom row) -->
        <com.google.android.flexbox.FlexboxLayout
            android:id="@+id/llLetters"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="14dp"
            android:paddingVertical="6dp"
            app:flexDirection="row"
            app:flexWrap="wrap"
            app:justifyContent="center"
            app:alignItems="center"
            app:alignContent="center" />

        <!-- 🎉 Play Again Heading -->
        <TextView
            android:id="@+id/tvPlayAgain"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:fontFamily="@font/poppins_bold"
            android:gravity="center"
            android:shadowColor="#F57C00"
            android:shadowDx="3"
            android:shadowDy="3"
            android:shadowRadius="8"
            android:text="PLAY AGAIN!"
            android:textColor="#FFA726"
            android:textSize="32sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Play Again Button -->
        <Button
            android:id="@+id/btnRestart"
            android:layout_width="220dp"
            android:layout_height="70dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/play_again_button_bg"
            android:elevation="10dp"
            android:fontFamily="@font/poppins_bold"
            android:text="Play Again!"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
